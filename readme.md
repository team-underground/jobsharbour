# Installation guide for rinvex - laravel subscription

## Install

`composer require rinvex/laravel-subscriptions`

## Execute migrations via the following command:

`php artisan rinvex:migrate:subscriptions`

## DB seed plans table

`php artisan db:seed`
